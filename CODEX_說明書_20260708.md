# CODEX 說明書 — 辰星官網維護筆記（2026-07-08）

這份是給未來 Codex / 本機維護者看的說明書。正式網站資料夾是：

`辰星網站-CMS版/上傳到GitHub`

## 目前我知道的狀態

辰星官網是靜態網站，主要內容包含首頁、服務頁、方案指南、Journal、貼文頁、相簿頁、CRM / OAuth 隱私相關頁面。CMS 發文資料在 `data/posts.json`，Journal 與 `posts/*.html` 由 `build-journal.js` 產生。

目前已做過：

- SEO / AI 搜尋強化：`office-guide.html`、`llms.txt`、FAQ / Article / LocalBusiness JSON-LD
- Journal / 貼文頁視覺優化
- 貼文圖片 fallback 防呆
- 多語系 `i18n.js` 強化
- 404 / 相簿頁語言切換
- `privacy.html` noindex 並 canonical 到 `privacy-policy.html`
- 相簿頁 base64 圖片抽出為外部圖片，HTML 大幅瘦身
- `dark.html` / `light.html` footer 隱私連結統一到 `privacy-policy.html`
- 2026-07-11 再次升級 `validate-site.js`，並修正文章頁延伸閱讀連結
- 2026-07-11 追加轉換率優化：首頁、三個服務頁、方案指南、Journal 與文章頁加入手機固定快速聯絡列
- 2026-07-11 續修：三個服務頁補上「適合 / 不適合 / 下一步」判斷卡片，`validate-site.js` 追加頁內錨點檢查
- 2026-07-11 續修 2：三個服務頁追加可見 FAQ 與 FAQPage 結構化資料；`i18n.js` 補英文 / 日文翻譯；`llms.txt` 補 AI 搜尋摘要用的客戶判斷重點

## 本輪 Codex 改了什麼

### 1. 修正驗證工具

檔案：

- `validate-site.js`

修正內容：

- 原本會把 `posts/*.html` 裡的 `../images/...` 誤判成缺圖。
- 現在會依照每個 HTML 的所在資料夾解析相對路徑。
- sitemap 檢查新增 `privacy-policy.html`、`terms.html`。
- 2026-07-11 升級：同步檢查 CSS/JS 資源、本機內部連結、相簿資料夾與相簿圖片數量。
- 這次升級抓到 `posts/*.html` 底部「延伸閱讀」連結錯誤，已在 `build-journal.js` 修正並重新產生文章頁。
- 2026-07-11 續修：新增頁內錨點檢查，例如 `index.html#contact`、`meeting-room.html#booking-wtc`，可避免 CTA 指到不存在的區塊。

### 2. 相簿頁瘦身

修改：

- `辰星場館相簿-信義館.html`
- `辰星場館相簿-世貿館.html`

新增：

- `images/gallery/xinyi/`
- `images/gallery/wtc/`

結果：

- 信義館相簿：從約 1.17 MB 降到約 12 KB
- 世貿館相簿：從約 1.23 MB 降到約 12 KB
- 兩頁各 9 張圖片已抽成外部圖片檔
- 頁面仍保留 `i18n.js` 語言功能

### 3. 手機固定快速聯絡列

修改：

- `assets/pages.css`
- `index.html`
- `dark.html`
- `light.html`
- `virtual-office.html`
- `private-office.html`
- `meeting-room.html`
- `office-guide.html`
- `build-journal.js`
- `journal.html`
- `posts/*.html`

目的：

- 手機版底部固定顯示「電話 / LINE / 預約」
- 降低客戶看完方案後找不到聯絡方式的流失
- Journal / posts 由 `build-journal.js` 自動產生，避免之後發文被覆蓋

### 4. 服務頁決策內容與視覺卡片

修改：

- `assets/pages.css`
- `virtual-office.html`
- `private-office.html`
- `meeting-room.html`
- `i18n.js`
- `llms.txt`

目的：

- 讓客戶快速判斷自己適合借址登記、獨立辦公室或會議室
- 補上「不適合誰」與「下一步確認事項」，降低諮詢前的不確定感
- 這類問答型內容同時有助於 SEO 長尾字與 AI 搜尋摘要引用
- 新增共用樣式 `.fit-grid`、`.fit-card`、`.next-step-note`
- 追加對應的可見 FAQ 與 JSON-LD FAQPage，讓搜尋引擎與 AI 摘要更容易引用
- 新增內容已補進 `i18n.js`，中英日切換有基本覆蓋
- `llms.txt` 新增「客戶判斷重點」，提供 AI crawler / AI 搜尋更清楚的方案選擇語境

## 每次修改後必跑

在 `上傳到GitHub` 資料夾執行：

```bash
node --check build-journal.js
node --check i18n.js
node --check validate-site.js
node validate-site.js
```

若修改 `data/posts.json`，先跑：

```bash
node build-journal.js
```

再跑驗證。

## 本輪測試結果

已通過：

```text
OK：檢查 20 個 HTML、JSON-LD、圖片路徑與 sitemap。
```

2026-07-11 升級後結果：

```text
OK：檢查 20 個 HTML、JSON-LD、圖片/資源/連結路徑、相簿資料夾與 sitemap。
```

2026-07-11 續修後結果：

```text
OK：檢查 20 個 HTML、JSON-LD、圖片/資源/連結/錨點路徑、相簿資料夾與 sitemap。
```

另確認：

- JSON-LD 可解析
- sitemap 包含 `office-guide.html`、`journal.html`、`/posts/`、`privacy-policy.html`、`terms.html`
- 兩個相簿頁已無 `src="data:image/..."`
- 相簿圖片路徑皆存在
- `posts/*.html` 延伸閱讀連結已無錯誤的 `href="posts/..."`
- 主要轉換頁與 Journal / posts 皆有 `mobile-contact-bar`
- 三個服務頁已有「適合 / 不適合 / 下一步」判斷卡片
- 三個服務頁新增 FAQ 已同步到 FAQPage JSON-LD，並通過 JSON-LD 解析

## 下次接手優先注意

1. 不要只上傳相簿 HTML，必須一起上傳：
   - `images/gallery/xinyi/`
   - `images/gallery/wtc/`

2. 若要改 Journal / 貼文頁版型，改：
   - `build-journal.js`
   - `assets/pages.css`

   不要只手動改 `journal.html` 或 `posts/*.html`，因為下次建置會覆蓋。

3. `dark.html` / `light.html` 已同步 footer 隱私連結到 `privacy-policy.html`；若正式站還會使用主題切換，請一併上傳這兩頁。

4. `i18n.js` 已經很大，之後若再擴充翻譯，建議拆分或至少分段整理。

## 目前最值得下一輪做的事

- 實機檢查手機版相簿與 Journal
- 上線後用手機實測固定聯絡列是否遮擋聊天泡泡或主題切換鈕
- 上線後人工切換 English / 日本語，確認三個服務頁新增判斷卡片與 FAQ 的語感是否符合品牌
- 進一步壓縮相簿圖片，例如 WebP / AVIF；目前本機沒有 `magick`、`cwebp`、`ffmpeg`，所以未執行轉檔
- 若取得 Google 商家與社群連結，補進 LocalBusiness / Organization JSON-LD 的 `sameAs`
