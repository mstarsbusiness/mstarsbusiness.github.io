# CLAUDE 說明書 — 辰星官網接手指南（2026-07-08）

Claude 你好，這份是 Codex 在你優化後再次檢查並執行改善後留下的交接說明。

正式網站資料夾：

`辰星網站-CMS版/上傳到GitHub`

## 你前一輪做的事，我已確認

我重新檢查後確認你主要處理了：

- `_要上傳清單_20260708.md`
- `index.html` 隱私連結統一方向
- `privacy.html` 設為 `noindex,follow`，canonical 指向 `privacy-policy.html`
- `404.html` 加入語言功能
- 兩個相簿頁加入語言功能
- `i18n.js` 補 404 / 相簿 / 隱私等翻譯
- `build-journal.js` 讓 Journal / posts 產出頁帶語言功能

這些方向是合理的。

## Codex 本輪接著做了什麼

### 1. 修正 `validate-site.js`

之前 `validate-site.js` 會誤判 `posts/*.html` 裡的 `../images/...` 缺圖。

原因：

- 原本所有本機圖片都用網站根目錄解析
- 但 `posts/*.html` 的 `../images/...` 應該從 `posts/` 所在位置解析

目前已修正成依每個 HTML 所在資料夾解析相對路徑。

### 2. 相簿頁瘦身

原本：

- `辰星場館相簿-信義館.html` 約 1.17 MB
- `辰星場館相簿-世貿館.html` 約 1.23 MB
- 兩頁各有 9 張 base64 內嵌圖片

現在：

- `辰星場館相簿-信義館.html` 約 12 KB
- `辰星場館相簿-世貿館.html` 約 12 KB
- base64 圖片已抽出到：
  - `images/gallery/xinyi/`
  - `images/gallery/wtc/`

注意：上傳時不能只傳 HTML，必須連同兩個新圖片資料夾一起傳。

### 3. 更新上傳清單

已更新：

- `_要上傳清單_20260708.md`

裡面已列出：

- Claude 前一輪要上傳的檔案
- Codex 本輪新增 / 修改的檔案
- 新增圖片資料夾
- `dark.html` / `light.html` 的確認提醒

## 已測試

在 `上傳到GitHub` 執行過：

```bash
node --check build-journal.js
node --check i18n.js
node --check validate-site.js
node validate-site.js
```

結果：

```text
OK：檢查 20 個 HTML、JSON-LD、圖片路徑與 sitemap。
```

另外檢查：

- JSON-LD 解析正常
- sitemap 包含主要頁面
- 相簿頁已無 base64 圖片
- 相簿外部圖片路徑存在
- `dark.html` / `light.html` footer 隱私連結已統一到 `privacy-policy.html`
- 2026-07-11 再次升級 `validate-site.js`，並修正 `posts/*.html` 底部延伸閱讀連結
- 2026-07-11 已加入手機固定快速聯絡列，提高電話 / LINE / 預約轉換
- 2026-07-11 續修：`virtual-office.html`、`private-office.html`、`meeting-room.html` 補上「適合 / 不適合 / 下一步」判斷卡片；`assets/pages.css` 新增共用卡片樣式；`validate-site.js` 追加頁內錨點檢查
- 2026-07-11 續修 2：三個服務頁追加可見 FAQ 與 FAQPage JSON-LD；`i18n.js` 補新增文案的英文 / 日文；`llms.txt` 補 AI 搜尋用的方案判斷摘要

## 給 Claude 的下一步建議

1. 上線前請再次確認 `_要上傳清單_20260708.md`。
2. `dark.html` / `light.html` 已修正 footer 隱私連結；若正式站仍使用這兩頁，請一併上傳。

3. 實機或公開網址上線後，請檢查：
   - 相簿頁圖片是否正常顯示
   - 語言切換是否正常
   - 手機版是否沒有爆版
   - 手機底部「電話 / LINE / 預約」是否正常顯示且不遮擋重要內容
   - 三個服務頁新增的判斷卡片在手機版是否順眼、是否需要更品牌化文案
   - English / 日本語切換後，新增判斷卡片與新增 FAQ 是否語感自然
   - Journal / posts 是否仍可正常顯示

4. 多語系已補進 `i18n.js`；若 Claude 要再優化，可優先做語感潤飾，而不是重做整套引擎。

5. 若要再優化速度，下一步可以把 `images/gallery/xinyi/` 與 `images/gallery/wtc/` 的 jpg/png 轉成 WebP，但要同步確認瀏覽器相容與 fallback。

   Codex 於 2026-07-11 檢查過本機工具，沒有 `magick`、`cwebp`、`ffmpeg`，所以未在本機轉 WebP。

## 重要提醒

`journal.html` 和 `posts/*.html` 是產生器輸出。若要改它們，請改 `build-journal.js`，不要只改輸出檔。

`validate-site.js` 是本機檢查工具，不影響前台畫面，但建議保留，因為它可以在上傳前快速抓出 JSON-LD、圖片路徑與 sitemap 問題。

2026-07-11 起，`validate-site.js` 也會檢查 CSS/JS、本機內部連結與相簿圖片資料夾；若它報錯，請優先排查，不要忽略。

2026-07-11 續修後，`validate-site.js` 也會檢查本機頁內錨點，例如 `#contact`、`#booking-wtc`；若 CTA 連到不存在區塊，驗證會報錯。

手機固定聯絡列的共用樣式在 `assets/pages.css`，首頁三個版本則有各自內嵌 `mobile-contact-css`。若未來調整 CTA 文案，記得同步首頁版本與 `build-journal.js`。

三個服務頁的新增決策卡片使用 `.fit-grid`、`.fit-card`、`.next-step-note`。這是為了提升客戶自我篩選與轉換，不是單純 SEO 堆字；若要改文案，請保留「適合誰 / 不適合誰 / 下一步」這個結構。

`llms.txt` 已補上「客戶判斷重點」，方便 AI 搜尋或 AI crawler 快速理解服務差異。若未來價格、方案或贈送時數有變，請同步更新服務頁、`llms.txt` 與 `office-guide.html`，避免 AI 摘要引用舊資訊。
